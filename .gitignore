# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Python (bake scripts)
__pycache__/
*.pyc
.venv/
venv/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Secrets / local env — never commit (keep a committed .env.example if needed)
.env
.env.*
!.env.example

# Vercel CLI (created by `vercel link` / `vercel dev`)
.vercel
